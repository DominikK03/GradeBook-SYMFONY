<header class="flex items-center justify-between h-16 px-4 bg-white border-b border-gray-200">
    <div class="flex items-center">
        <button class="md:hidden p-2 rounded-md text-gray-500 hover:bg-gray-100">
            <i class="fas fa-bars"></i>
        </button>
        <h2 class="ml-2 text-lg font-semibold text-gray-800">
            {{ TopNavbarTitle|trans }}
        </h2>
    </div>
    <div class="flex items-center space-x-4">
        <button class="p-2 rounded-full text-gray-500 hover:bg-gray-100">
            <i class="fas fa-bell"></i>
        </button>
        <button class="p-2 rounded-full text-gray-500 hover:bg-gray-100">
            <i class="fas fa-envelope"></i>
        </button>
            <div class="relative">
                <div class="">
                    <a class="" href="{{ path('app_logout') }}">
                        {% include 'utils/exit.html' %}
                        <h1 class="text-xs">Wyloguj</h1>
                    </a>
                </div>
            </div>

    </div>
</header>
